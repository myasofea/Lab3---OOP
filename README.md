# Lab 3 — Matrix & Vector System

This project implements a matrix and vector system using C# with OOP concepts such as encapsulation, composition, aggregation, and dependency.

## Features
- Create and manipulate matrices
- Perform vector operations
- Demonstrates object-oriented principles

## How to Run
1. Open the solution in Visual Studio
2. Build and run the project
